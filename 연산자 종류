# 논리연산자
&& (AND 연산 "~이면서, ~그리고")

- A && B
: AB 둘다 ture 일때, 결과가 ture

|| (OR 연산 A 또는 B가 true 일때 결과가 true)

변수 ++ (후위 연산자)


++ 변수 (전위 연산자)

ex) 

let a =10
let b = 20
let C

c = a++ +b (연산자 3개 (3 ++) (1 +) (2 = ))
// 
// c = ? 
// 변수 ++ (후위 연산) : 연산우선순위 가장 낮다.

------------------------------------------------------

let a =10
let b = 20
let C

c = ++a + b
// c = ?
// ++변수 (전위 연산): 연산 우선순위 가장높다.
